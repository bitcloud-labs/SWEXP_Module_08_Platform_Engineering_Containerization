# Capstone monorepo dependency edge list (consumer -> dependency). See README.md.
# TODO: declare the Forge graph. apps may depend on packages; packages must NOT
#       depend on apps; no cycles. (A node with nothing after -> has no deps.)
