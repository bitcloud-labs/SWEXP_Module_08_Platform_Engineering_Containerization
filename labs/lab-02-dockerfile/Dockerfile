# Lab 02 — hygienic Dockerfile for the Forge API. See README.md.
#
# TODO 1: pin the base image (NOT :latest).            e.g. FROM node:22.13-bookworm-slim
# TODO 2: WORKDIR /app
# TODO 3: COPY package*.json ./   then   RUN npm ci    (BEFORE copying the source)
# TODO 4: COPY . .
# TODO 5: USER node               (run as non-root)
# TODO 6: HEALTHCHECK ...
# TODO 7: CMD ["node", "server.js"]
#
# Do NOT bake secrets in via ENV/ARG (no *PASSWORD* / *SECRET* / *TOKEN* / *API_KEY*).
