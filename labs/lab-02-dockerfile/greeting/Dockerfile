# Lab 02 — a tiny image that is actually built and run by the grader.
# It must print exactly:  forge-up
#
# TODO: base it on a small image (alpine or busybox) and set a CMD that prints forge-up.
#       e.g.
#         FROM alpine:3.20
#         CMD ["echo", "forge-up"]
