# Lab 00 — a first hygienic Dockerfile. See README.md.
#
# TODO 1: pin the base image (a real tag, NOT :latest, NOT untagged).
#         e.g.  FROM node:22.13-bookworm-slim
#
# TODO 2: set up the app (WORKDIR, copy package*.json, install, copy source).
#
# TODO 3: run as a NON-root user (e.g.  USER node).
#
# TODO 4: declare a HEALTHCHECK.
#
# TODO 5: set the CMD that starts the service.
